# 세팅 절차

처음 한 번은 2~3시간, 그 뒤 매달 15분. 코딩 몰라도 된다. 터미널 명령은 AI가 대신 친다.

## 0. 준비 (15분)

1. 이 폴더를 받는다: `git clone … personal-cashflow-kit`
2. Codex 앱(또는 Claude Code)에서 이 폴더를 연다. `AGENTS.md`가 자동으로 읽힌다.
3. 뱅크샐러드 앱 → 마이 → **데이터 내보내기 → 파일로 받기**, 기간 **최근 1년**, 압축 비밀번호를 정한다. 메일로 온다.
4. AI에게 **"세팅 시작"** 이라고 말한다. AI가 먼저 **사용자 이름과 뱅크샐러드 내보내기 메일을 받은 Gmail 주소**를 한 번 묻는다. 이메일 주소는 로그인 계정 확인에만 쓰고 파일에는 저장하지 않는다.
5. 답하면 AI가 `sh scripts/fetch.sh --wait`를 돌린다. 설치, Gmail 세션 확인, 뱅샐 메일 찾기, zip 받기·풀기, 진단까지 혼자 한다. 이후 사람이 하는 건 크롬 창이 뜰 때 Gmail 로그인, 필요할 때 압축 비밀번호 입력뿐이다.

수집이 끝나면 **인터뷰 전에 최근 3개 완성월의 생활 윤곽부터** 나온다. 추정 월평균 수입, 카테고리별 지출, 생활·쇼핑, 단순히 남은 돈을 먼저 보고 인터뷰로 숫자를 정확하게 만든다. 장기 투자·주택 시나리오는 인터뷰가 끝난 뒤 본다.

파일을 직접 넣어도 된다: xlsx를 `private/exports/`에 두면 AI가 수집 단계를 건너뛴다. 처음부터 다시 하려면 `sh scripts/reset.sh` (데이터는 `private/_archive/`로 옮겨진다).

> 뱅샐 export에는 이름·신용점수·전 계좌 잔액·보험·대출이 다 들어 있다. `private/`는 git이 무시하지만, 이 폴더를 통째로 클라우드에 올리지는 말 것.

## 1. 진단 (AI, 1분)

`python3 scripts/inspect_export.py` → `private/setup/` 에 세 파일:

- `01_진단.md` — 기간·건수, 결제수단 목록, 자산·부채·대출, 반복 거래, 미분류 상위 가맹점, 사람에게 간 이체
- `profile.draft.yaml` — 계좌마다 종류를 추정해 채운 프로필 초안 (`_hint`에 근거)
- `02_질문지.md` — 최근 3개월 카테고리 요약 → A 가구 → B 계좌·카드 → C 월 반복/연 1~2회 지출 → D 사람 송금 → E 남은 큰 미분류 → F 수입원 → G 종목별 귀속 → H 주거 형태. 추정과 실제 날짜·금액을 보고 틀린 것만 고친다.

AI는 진단 요약을 먼저 보여준다. "계좌 27개, 미분류 122건 580만원, 사람 송금 상대 12명" 같은 식으로.

## 2. 인터뷰 (사람 + AI, 최대 20문) ← 세팅의 본체

AI가 `02_질문지.md` 순서로 **한 번에 하나씩** 묻는다. 큰 이체·반복비·수입처럼 결과값을 바꾸는 항목을 우선하고, 그 외 미분류는 확인필요로 남겨 대시보드와 월 요약을 먼저 만든다. 예:

> **KB종합통장-저축예금** (checking) — 거래 290건, 수입 3,746만, 이체 4.9억. 누구 것이고 성격은? (생활 / 저축 / 사업 / 위탁 / 제외)

답은 짧게: "내 월급통장", "엄마 카드라 빼줘", "이건 절반 엄마 돈", "저 사람은 전 집주인, 보증금 돌려받은 거".
AI는 답을 즉시 `private/profile.yaml`(계좌·가족·고정비·수입원)과 `private/rules.csv`(가맹점 → 카테고리)에 적는다.

요령:
- AI가 먼저 추정하고 사람은 **틀린 것만** 고친다. "전부 생활용인가요?" 같은 0에서 시작하는 질문은 없다.
- **B(계좌)·C(고정비)·D(사람 송금)가 핵심**. 여기만 정확하면 거래의 80%가 자동으로 맞는다.
- 식사·카페·교통처럼 뱅샐이 이미 분류한 것과 편의점·구독·보험처럼 공통 룰로 잡히는 가맹점은 묻지 않는다.
- E~G는 "맞아/아니 이건 ○○" 수준으로 빠르게.
- 모르는 건 "몰라"라고 답해도 된다. 확인필요로 남고 다음 달에 다시 묻는다.
- 엄마 돈·형제 돈·모임비처럼 **내 돈이 아닌 것**은 이때 말해야 한다. 자산이 부풀려지는 것을 막는 유일한 지점이다.
- 주식·ETF·펀드는 총액을 다시 입력하지 않고 export의 종목별로 누구 돈인지 확인한다. 주택은 자가/전세/월세/가족 집/회사 제공 중에서 고르고, 공동명의·위탁 여부가 있을 때만 보충한다.

## 3. 빌드 + 잔여 확인 (AI, 30분)

```
sh scripts/run.sh     # = build_ledger.py + summarize.py
```

- `private/setup/03_확인필요.md` 에 아직 못 정한 거래가 금액순으로 묶여 나온다.
- 그 전에 AI가 먼저 추측한다(가맹점 이름으로 업종이 분명하면 신뢰도 높게, 송금·PG사처럼 알 수 없으면 낮게). 신뢰도 0.6 이상만 적용되고 "AI 추정" 표로 보여준다. 사람은 틀린 것만 고치고, 맞으면 룰로 승격돼 다음 달부터는 안 묻는다.
- 남은 것만 AI가 큰 것부터 묻는다. 가맹점 단위 답은 `rules.csv`, 한 건짜리는 `ledger/overrides.csv`에 적힌다.
- 목표: 확인필요 잔여 금액이 **월지출의 5% 아래**. 0건을 만들 필요 없다.

## 4. 결과 확인 + 거울 (사람, 15분)

인터뷰가 끝나면 `private/out/insight.md`가 생긴다. 교과서가 아니라 거울이다. 내 거래에서 나온 숫자로만 말한다.

- **한 달에 실제로 남는 돈**과 그중 어디로도 안 가고 통장에 쌓이는 돈
- **이 페이스로 1·5·10년** 뒤 종잣돈, 월 +30만·+50만이면 어떻게 달라지는지
- **집**: 목표 집값 기준 내 돈이 얼마 필요하고 지금 얼마 있고 몇 년 걸리는지, 사고 나면 월 원리금이 얼마인지. 거리가 멀면 "절약 문제가 아니라 소득 문제"라고 그대로 말한다
- **투자하는 방식**: 적극형/관망형 관찰, 개별주 비중이 크면 하락장에 사라질 금액, 놀고 있는 현금
- **바꿀 수 있는 손잡이**: 구독·외식·늘어난 항목이 위 시나리오를 얼마나 바꾸는지 (강요 아님)
- **한 줄**

투자 기초 정석(재무제표 자동 채움 엑셀, 저축률·체크리스트·코어/위성)은 `insight_투자참고.md`에 따로 있다. 보고 싶을 때만.

## 4-1. 숫자 읽는 법

먼저 `private/out/dashboard.html` 을 브라우저로 연다. 가계부 탭에서 월별 요약표·카테고리별 지출 흐름표·추이 차트를, 자산 탭에서 자산 구성·증권사별 투자·종목·대출 상세를 볼 수 있다. 이 파일은 로컬에서만 열리고 매달 `run.sh` 때 새로 만들어진다.

`private/out/financial_profile.md` 를 읽는다. 여기 나오는 것:

- 월 평균 수입·지출·고정비 비율·순현금흐름·저축률 (완전한 달만 평균)
- 월별 표 (저축·투자 이체, 자산거래, 확인필요는 따로)
- 지출 카테고리 월평균 상위
- 자산·부채·순자산, 유동자산으로 몇 달 버티는지, 대출 금리·만기, 투자 원금 대비 수익률, 보험 건수
- 위탁(남의 돈)·제외 계좌·프로필에 없는 자산 항목

숫자가 이상하면 원인은 거의 셋 중 하나: ① 계좌 귀속이 틀림 ② 큰 이체가 지출로 잡힘(→ `자산거래`) ③ 짝 없는 내부이체(→ 계좌 `aliases`). AI에게 "○월 지출이 왜 이렇게 커?"라고 물으면 ledger에서 찾아 준다.

## 매달 (15분)

1. 뱅샐 앱에서 "파일로 받기" — 기간은 **최근 1년** 그대로 (겹치는 거래는 자동으로 파일 간 중복 제거)
2. AI에게 **"이번 달 마감"** → AI가 `sh scripts/fetch.sh 2026_09`로 뱅샐 메일·쿠팡 메일·네이버페이 내역을 받는다
3. AI가 `run.sh` → 새 가맹점·새 이체 상대만 묻는다 (보통 5~10개, 쿠팡·네이버 건은 품목이 붙어 나와 답이 쉽다) → 다시 `run.sh`
4. `financial_profile.md` 갱신 확인. AI 재무 도우미에 연결돼 있으면 이 파일이 새 재무 프로필이다.

## 다음 스텝 — 투자·집 설계

대시보드의 **다음 설계** 탭은 지출을 10%·20% 조정했을 때의 후보와 현재 투자·연금·주택 상태를 한 화면에 보여준다. 여기서 바로 정답 비율을 만들지 않고, AI가 `05_투자설계_질문지.md`를 따라 한 번에 하나씩 묻는다. 답은 프로필에 남아 다음 대화와 다음 달 마감 때 이어진다. 인터뷰가 끝나면 현금 안전판·집/대출·연금·일반계좌의 월 배분표와 다음 점검일을 만든다.

## AI 재무 도우미에 넘기는 것 / 안 넘기는 것

| 넘김 | 안 넘김 |
|---|---|
| `out/financial_profile.md` (월 평균·비율·자산 구조) | `exports/*.xlsx` (원본) |
| `out/cashflow_monthly.csv`, `balance_sheet.json` | `ledger/ledger.csv` (거래 한 줄 한 줄) |
| | `profile.yaml` (계좌명·가족 관계) |

"민낯"은 `private/`에 남고, AI 재무 도우미은 **형편만** 안다. 이게 이 킷의 약속이다.
